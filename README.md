## Available Scripts

In the project directory, you can run:
<br/>
To start the server
### `cd server`
### `npm install`
### `node index.js`

To start the client
### `cd curd`
### `npm install`
### `npm start`
